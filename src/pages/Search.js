import React from 'react';
import { withTheme } from 'styled-components';

function Search() {
	return (
        <div>
            <h1>Søk</h1>
        </div>
    );
}

export default withTheme(Search);
