import React from 'react';
import { withTheme } from 'styled-components';

function Summary() {
	return (
        <div>
            <h1>Sammendrag</h1>
        </div>
    );
}

export default withTheme(Summary);
