import React from 'react';
import { withTheme } from 'styled-components';

function Profile() {
	return (
        <div>
            <h1>Profil</h1>
        </div>
    );
}

export default withTheme(Profile);
